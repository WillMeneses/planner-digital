export const APP_CONFIG = {
    // 'LOCAL' = IndexedDB (Offline only)
    // 'CLOUD' = Azure Functions + Cosmos DB
    DATA_MODE: 'CLOUD',

    API_URL: '/api' // Azure SWA Proxies /api calls automatically
};
